"""Unittests for solentware_base.api package.

"PACKAGE CONTENTS" is incomplete because some modules do not have a unittest
and because some of the unittests which do exist are stubs.

Unittests exist for:

find
where
"""
