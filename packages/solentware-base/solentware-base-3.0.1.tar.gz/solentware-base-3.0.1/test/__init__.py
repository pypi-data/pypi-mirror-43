"""Unittests for solentware_base package.

"PACKAGE CONTENTS" is incomplete because some modules do not have a unittest
and because the unittests which do exist are stubs.

Unittests, which are not stubs, exist for:

apswapi
apswduapi
dbapi
sqlite3api
sqlite3duapi

"same_interface_test" compares the interfaces to the installed database engines.

"""
