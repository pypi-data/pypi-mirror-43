class InternalServices(object):  # noqa
    DOCKERIZER = 'dockerizer'
    INITIALIZER = 'initializer'
    SIDECAR = 'sidecar'
    HELPER = 'helper'
    RUNNER = 'runner'

    VALUES = [
        DOCKERIZER,
        INITIALIZER,
        SIDECAR,
        HELPER,
        RUNNER
    ]
