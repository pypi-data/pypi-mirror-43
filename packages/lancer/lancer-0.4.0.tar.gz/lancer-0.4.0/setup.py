#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['lancer']

package_data = \
{'': ['*']}

install_requires = \
['attrs>=18.1.0',
 'secretly>=0.2',
 'treq>=18.6.0',
 'txacme[libcloud]>=0.9.2',
 'twisted[tls]>=18.7.0']

entry_points = \
{'console_scripts': ['lancer = lancer._impl:script']}

setup(name='lancer',
      version='0.4.0',
      description='Provision TLS *Cer*tificates for your *LAN*, using the ACME DNS challenge.',
      author='Glyph',
      author_email='glyph@twistedmatrix.com',
      url='https://github.com/glyph/lancer',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
