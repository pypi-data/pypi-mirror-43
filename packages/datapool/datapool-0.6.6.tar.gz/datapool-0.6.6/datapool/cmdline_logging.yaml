---

version: 1


formatters:

    simple:
        format: "  %(levelname)s - %(message)s"
handlers:

    console:

        class: logging.StreamHandler
        level: DEBUG
        formatter: simple
        stream: ext://sys.stdout

loggers:

    eawag.datapool:

        level: ERROR
        handlers: [console]
        propagate: no
