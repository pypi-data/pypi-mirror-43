#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['pystubber']

package_data = \
{'': ['*']}

entry_points = \
{'console_scripts': ['pystubber = pystubber.cli:main']}

setup(name='pystubber',
      version='0.1.2',
      description='pystubber - Creates a stub python file from a python module',
      author='Alan Justino da Silva',
      author_email='alan.justino@yahoo.com.br',
      url='https://github.com/alanjds/pystubber',
      packages=packages,
      package_data=package_data,
      entry_points=entry_points,
      python_requires='~=3.5',
     )
