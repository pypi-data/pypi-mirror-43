import io
import os
from setuptools import setup, find_packages

setup(name='reprozip-web',
      version='0.0',
      py_modules=['reprozip_web'],
      description="Allows the ReproZip unpacker to record and replay data journalism websites packaged as .rpz files")
