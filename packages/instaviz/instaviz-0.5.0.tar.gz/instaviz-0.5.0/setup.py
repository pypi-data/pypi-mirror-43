#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['instaviz']

package_data = \
{'': ['*'], 'instaviz': ['static/*', 'templates/*']}

install_requires = \
['bottle', 'jinja2', 'pygments']

setup(name='instaviz',
      version='0.5.0',
      description='InstaViz - a tool for visualizing ASTs and CPython code objects in a web server.',
      author='Anthony Shaw',
      author_email='anthonyshaw@apache.org',
      url='https://github.com/tonybaloney/instaviz',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
     )
