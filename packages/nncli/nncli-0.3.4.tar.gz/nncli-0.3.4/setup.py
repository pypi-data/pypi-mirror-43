#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['nncli']

package_data = \
{'': ['*']}

install_requires = \
['urwid', 'requests', 'appdirs', 'click']

extras_require = \
{'dev': ['pipenv'], 'doc': ['sphinx']}

entry_points = \
{'console_scripts': ['nncli = nncli.cli:main']}

setup(name='nncli',
      version='0.3.4',
      description='NextCloud Notes Command Line Interface',
      author='Daniel Moch',
      author_email='daniel@danielmoch.com',
      url='https://nncli.org',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3',
     )
